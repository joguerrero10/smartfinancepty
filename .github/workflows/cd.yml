name: CD - Deploy to Production

on:
  push:
    branches: [main]
    tags: ['v*.*.*']

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/backend

jobs:
  # ── Build y Push a GitHub Container Registry ──────────────────────────────────
  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
      image-digest: ${{ steps.build.outputs.digest }}

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Login a GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extraer metadata para Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=tag
            type=sha,prefix=sha-
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Configurar Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build y Push imagen
        id: build
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ── Deploy a servidor ─────────────────────────────────────────────────────────
  # deploy:
  #   name: Deploy to Production
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   environment: production

  #   steps:
  #     - name: Checkout código
  #       uses: actions/checkout@v4

  #     - name: Deploy via SSH
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.SERVER_HOST }}
  #         username: ${{ secrets.SERVER_USER }}
  #         key: ${{ secrets.SERVER_SSH_KEY }}
  #         port: ${{ secrets.SERVER_PORT || 22 }}
  #         script: |
  #           # Navegar al directorio del proyecto
  #           cd /opt/smartfinancepty

  #           # Login al registry
  #           echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

  #           # Pull de la nueva imagen
  #           docker compose pull backend

  #           # Recrear solo el backend (zero-downtime si hay múltiples réplicas)
  #           docker compose up -d --no-deps --build backend

  #           # Verificar que el health check pase
  #           sleep 30
  #           docker compose ps backend

  #           # Limpiar imágenes viejas
  #           docker image prune -f

  #     - name: Verificar deployment
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.SERVER_HOST }}
  #         username: ${{ secrets.SERVER_USER }}
  #         key: ${{ secrets.SERVER_SSH_KEY }}
  #         script: |
  #           response=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/actuator/health)
  #           if [ "$response" = "200" ]; then
  #             echo "✅ Deploy exitoso - Backend respondiendo"
  #           else
  #             echo "❌ Deploy fallido - Health check retornó $response"
  #             docker compose logs backend --tail=50
  #             exit 1
  #           fi

  #     - name: Notificar éxito
  #       if: success()
  #       run: |
  #         echo "🚀 Deploy exitoso a producción"
  #         echo "Imagen: ${{ needs.build-and-push.outputs.image-tag }}"
  #         echo "Commit: ${{ github.sha }}"
